class Order {
    
    // props = {id, showing, ticketList}
    constructor(showing, bookingInfo) {
        this.showing = showing;
        // todo
        this.bookingInfo = bookingInfo;
    }
}

module.exports = Order;